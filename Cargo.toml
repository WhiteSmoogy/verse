[package]
name = "verse-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.8"
tokio = { version = "1", optional = true, features = ["rt", "time"] }

[features]
default = []
tokio-host = ["dep:tokio"]
